export enum AppRoutes {
  Members = '/members',
  Applications = '/applications',
  AllGroups = '/all-groups',
  CreateGroup = '/all-groups/create',
  
  Login = '/login',
  Register = '/register/:code',
}
