export enum RoleEnum {
  Guest = 'guest',
  Owner = 'owner',
  Admin = 'admin',
  Student = 'student',
  Watcher = 'watcher',
}

export enum GroupRoleEnum {
  Member = 'member',
  Admin = 'admin',
  Moderator = 'moderator'
}
